using VContainer;
using VContainer.Unity;

namespace Binh.Scenes
{
    public class SceneBootstrapLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
